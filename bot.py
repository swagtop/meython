from meython import geq

'''
lift, roll, above, lil_meyer, meyer = 0, 1, 31, 21

def bot(previous: int, roll: int, state: int): -> int:
    match state:
        'Your bot has just received the cups, from the previous bot'
        case 0:
            if previous == meyer:
                return lift
            else:
                return roll
        
        'Your bot has rolled the dice'
        case 1:
            if geq(roll, previous):
                return roll
            else:
                return above

        case 2:
'''

